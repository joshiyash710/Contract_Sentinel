# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# Distribution / packaging
*.egg-info/
dist/
build/

# Test cache
.pytest_cache/
.coverage
htmlcov/

# Type checker
.mypy_cache/

# IDEs
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Generated report artifacts (ReportAgent / Node 7 output — runtime-created, never committed)
backend/data/reports/

# Uploaded contract files (runner/API upload dir — runtime-created, never committed)
backend/data/uploads/

# OAuth credentials and tokens (MCP delivery — never committed, D10 / D9)
backend/data/secrets/

# SQLite durable stores (feature 012 — runtime-created, never committed)
backend/data/*.db
backend/data/*.db-wal
backend/data/*.db-shm