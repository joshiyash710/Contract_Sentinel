import { Card } from "@/components/ui/Card";

export default function DashboardPage() {
  return (
    <Card>
      <h2 className="text-h3 font-semibold">Dashboard</h2>
      <p className="text-body text-text-secondary">
        Placeholder route — the Command Center screen is built in spec 018.
      </p>
    </Card>
  );
}
