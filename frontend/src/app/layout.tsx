import type { Metadata } from "next";
import "./globals.css";
import { AppShell } from "@/components/shell/AppShell";

export const metadata: Metadata = {
  title: "ContractSentinel",
  description: "Autonomous contract-risk-analysis agent",
};

// Inter is loaded via a plain stylesheet link (runtime, non-blocking) rather than
// next/font/google, so the production build never blocks on a font fetch. The --font-inter
// token (globals.css) resolves to Inter when loaded, system-ui otherwise.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>
        <AppShell>{children}</AppShell>
      </body>
    </html>
  );
}
